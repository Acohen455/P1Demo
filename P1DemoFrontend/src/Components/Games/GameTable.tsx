import {Container} from "react-bootstrap";

export const GameTable:React.FC = () => {
    return(
        <Container>
            <h1>This is where the games would be... if I had any!</h1>
        </Container>
    )
}